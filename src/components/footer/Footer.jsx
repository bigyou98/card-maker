import React, { memo } from "react";
import style from "./footer.module.css";

const Footer = memo(() => {
  return <footer className={style.footer}>Dreams Come True</footer>;
});

export default Footer;
